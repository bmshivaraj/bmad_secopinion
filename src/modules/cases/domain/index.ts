// Cases module domain entities and business rules land here, starting Epic 3.
// This is also where the CaseStatus state machine (AD-13) will be defined as
// the single source of truth every other module imports.
export {};
