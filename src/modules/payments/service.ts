// Payments module's public service interface (AD-2 seam). All payment/payout/
// refund operations go through the PaymentProvider abstraction (AD-3), never
// gateway-specific code. Implemented starting Epic 3/5.
export const paymentsService = {};
