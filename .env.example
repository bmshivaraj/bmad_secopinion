# Postgres connection string used by the app and the Prisma CLI.
DATABASE_URL="postgresql://secopinion:secopinion@localhost:5432/secopinion"

# Secret used to sign/verify JWT auth-context tokens. Use a long random string.
JWT_SECRET="replace-with-a-long-random-string"

NODE_ENV="development"
