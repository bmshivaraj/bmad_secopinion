// Cases module's public service interface (AD-2 seam). Cases orchestrates a
// case's lifecycle by calling Patients, Doctors, and Payments through their
// own service.ts files. Implemented starting Epic 3.
export const casesService = {};
